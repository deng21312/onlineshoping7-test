name: 拉取Gitee代码-纯测试
on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: 拉取Gitee代码
        uses: actions/checkout@v4
        with:
          repository: https://gitee.com/ZhiheZier_0719/online-shopping-system.git
          ref: dev